// Create an array which contains all the available datatypes 
//     * print datatype of every element in the array using forEach

console.log("datatypes :\n")
let arr = ["js",true,12,87.736];
arr.forEach(element => {
    console.log(typeof(element))
    
});
